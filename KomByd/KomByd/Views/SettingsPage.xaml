<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:renderers="clr-namespace:AiForms.Renderers;assembly=SettingsView"
             x:Class="KomByd.Views.SettingsPage">
    <ContentPage.Content>
        <renderers:SettingsView HasUnevenRows="True" SeparatorColor="Transparent" HeaderTextColor="{StaticResource HeaderColor}" HeaderFontSize="20">
            <renderers:Section Title="Ustawienia ogólne">
                <!--<renderers:PickerCell Title="Wybierz język aplikacji"
                                      Description="{Binding Language}"
                                      ItemsSource="{Binding LanguageList}"
                                      MaxSelectedNumber="1" 
                                      KeepSelectedUntilBack="true"
                                      PageTitle="Wybierz język" />-->
            </renderers:Section>
            <renderers:Section Title="Zarządzanie danymi i aktualizacjami">
                <renderers:LabelCell Title="Wersja bazy danych" Description="{Binding DatabaseVersion}"/>
            </renderers:Section>
        </renderers:SettingsView>
    </ContentPage.Content>
</ContentPage>